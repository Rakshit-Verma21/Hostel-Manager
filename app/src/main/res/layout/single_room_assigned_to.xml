<?xml version="1.0" encoding="utf-8"?>
<androidx.cardview.widget.CardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="100dp"
    android:layout_height="150dp"
    android:layout_margin="10dp"
    app:cardCornerRadius="20dp"
    app:cardElevation="5dp">

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <ImageView
            android:id="@+id/single_room_image"
            android:layout_width="100dp"
            android:layout_height="60dp"
            android:background="@drawable/background_inside"
            android:cropToPadding="true"
            android:scaleType="fitXY">

        </ImageView>

        <TextView
            android:id="@+id/single_room_assigned_to_name"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_below="@id/single_room_image"
            android:layout_marginTop="5dp"
            android:text="Kassandra"
            android:textAlignment="center"
            android:textColor="#000000"
            android:textSize="16sp"></TextView>

        <Button
            android:id="@+id/remove_from_room_button"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_below="@id/single_room_assigned_to_name"
            android:layout_marginLeft="4dp"
            android:layout_marginTop="10dp"
            android:layout_marginRight="4dp"
            android:background="@drawable/background_text"
            android:text="Remove" />

    </RelativeLayout>

</androidx.cardview.widget.CardView>